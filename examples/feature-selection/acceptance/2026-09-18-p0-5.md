# 特征筛选试点 · P0-5A 独立交付验收

日期：2026-09-18
范围：`sens-prototype` 独立交付能力，不包含 `sens-preview` 修改，不包含真实外部发布。

## 结论

P0-5A 通过。当前 `sens-prototype` 仓库在不携带现成 `node_modules` 和 `dist` 的情况下，可以从干净副本重新安装依赖、完成生产构建，并通过 HTTP 正常加载构建后的 HTML、JavaScript 和 CSS。

P0-5 尚未全部完成。P0-5B 的真实团队访问链接仍为 `not_published`，需要接入目标 GitLab 仓库后继续。

## 独立构建

执行：

```bash
node scripts/verify-portable-delivery.mjs
```

实际结果：

- 干净副本：通过；复制时排除 `.git`、`node_modules` 和 `dist`。
- 依赖安装：通过；在临时副本中重新安装 93 个包。
- TypeScript：通过。
- Vite 生产构建：通过，共转换 1508 个模块。
- 构建产物：`index.html`、1 个 CSS、1 个 JavaScript。
- 临时副本：验收结束后自动删除。

## 静态资源

- `index.html`：HTTP 200。
- 构建后的 CSS：HTTP 200。
- 构建后的 JavaScript：HTTP 200。
- Vite 使用 `base: './'`，资源引用为相对路径，可部署在 GitLab 子路径。
- 页面当前为单入口状态原型，不存在需要服务端回退的前端路由。

## 路径与依赖边界

- 构建不读取 `sens-preview` 源码。
- Sens 能力来自仓库内固定资产 `vendor/sens-prototype-kit-0.1.0-p0.6.tgz`。
- 构建产物和 `prototype-spec.json` 中未发现个人主目录绝对路径。
- 原始录屏只保留为外部资料名称和用途，不随仓库分发。
- 目标机器仍需要 Node.js、npm，并能访问 npm 软件源以安装 React、Vite 等公开依赖。

## 已知问题

- JavaScript 构建产物约 3.13 MB，Vite 报告大于 500 kB 的包体积告警；不阻塞 P0-5A，但应进入后续拆包与 tree-shaking 事项。
- antd 的 `use client` 指令在 Vite 构建中被忽略并产生提示；构建成功，不属于业务层直接使用 antd。
- 本轮只验证可迁移构建和静态资源，没有重复执行 P0-4 的全部业务交互路径。
- 尚无真实 GitLab Pages 或团队预览地址，P0-5B 未完成。

## 状态

| 项目 | 状态 |
| --- | --- |
| 干净副本安装 | pass |
| 生产构建 | pass |
| HTML / JS / CSS 资源 | pass |
| 个人绝对路径检查 | pass |
| 不依赖 `sens-preview` 源码 | pass |
| 真实分享链接 | not_published |
| 设计师最终审核 | awaiting_designer |
