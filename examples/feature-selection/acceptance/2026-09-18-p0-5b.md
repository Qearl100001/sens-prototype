# 特征筛选试点 · P0-5B 公开发布验收

日期：2026-09-18

## 结论

P0-5B 通过。`sens-prototype` 已作为独立公开仓库发布，GitHub Actions 能在公共 npm 环境完成安装与生产构建，GitHub Pages 链接可访问，线上 JavaScript 与 CSS 均加载成功，Chrome 实际渲染页面正常。

- 代码仓库：<https://github.com/Qearl100001/sens-prototype>
- 公开原型：<https://qearl100001.github.io/sens-prototype/>
- 成功部署：<https://github.com/Qearl100001/sens-prototype/actions/runs/35330054972>
- 发布提交：`f2bce9ba78cf68cd3ec78b3a8b321fa322721b4f`

## 构建与部署

- GitHub Actions 拉取仓库：通过。
- Node.js 20 初始化：通过。
- `npm ci`：通过，依赖来自公共 npm registry。
- TypeScript 与 Vite 生产构建：通过。
- GitHub Pages artifact 上传：通过。
- GitHub Pages 部署：通过。
- Pages 构建模式：`workflow`。

第一次工作流的应用构建已经通过，但由于仓库尚未启用 Pages，配置步骤失败；启用 GitHub Pages 后重新触发，同一工作流完整通过。该初次失败不属于页面或依赖构建失败。

## 线上资源

| 资源 | 状态 | 内容类型 |
| --- | --- | --- |
| 页面入口 | 200 | `text/html` |
| JavaScript | 200 | `application/javascript` |
| CSS | 200 | `text/css` |

页面标题为“特征值单选与多选试点”。Chrome 以 1440 × 1000 打开公开链接后，顶部导航、页面标题、基础信息表单和模板详情区域均正常渲染，不是空白页。

## 公开交付边界

- 用户已明确确认仓库可以公开。
- 原始 PRD 和屏幕录制没有进入仓库。
- 个人主目录绝对路径已清理。
- Git 提交使用 GitHub noreply 邮箱，不暴露公司邮箱。
- 三份锁文件不再引用公司内部 npm 地址。
- `sens-preview` 未进入该仓库，也未因本轮发布被修改。

## 已知问题

- `npm ci` 报告 1 个中危和 1 个高危依赖项；本轮未执行可能引入破坏性升级的 `npm audit fix --force`，需要后续单独评估。
- JavaScript 构建产物约 3.13 MB，仍有 Vite 包体积告警。
- 本轮验证的是独立构建、公开访问和首屏渲染；完整业务交互自动验收属于 P0-6。
- 设计师最终审核仍为 `awaiting_designer`。

## 状态

| 项目 | 状态 |
| --- | --- |
| 独立公开仓库 | pass |
| 公共 npm 构建 | pass |
| GitHub Actions | pass |
| GitHub Pages | pass |
| HTML / JS / CSS | pass |
| Chrome 首屏渲染 | pass |
| 完整业务交互自动验收 | P0-6 |
| 设计师最终审核 | awaiting_designer |
